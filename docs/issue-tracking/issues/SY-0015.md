---
type: Issue
id: SY-0015
title: MCP/tool configuration
description: Configure repository MCP allowlists for Context7, Cloudflare docs, and specialized Playwright.
status: in_review
priority: P2
estimate: null
assignee: agent
project: stage-1
milestone: Stage 1
cycle: stage-1
rank: null
tags: [task]
parent: SY-0008
blocked_by: [SY-0009, SY-0014]
blocks: [SY-0016]
relates: []
resource: docs/roadmap/implementation-roadmap.md
linear_id: null
branch: shreyas/sy-0014-agent-instructions
pr: null
created: 2026-08-19
timestamp: 2026-08-20T00:00:00Z
stage: 1
security_impact: low
---

## Objective

Create the runnable monorepo skeleton, governance, agent system and CI foundation without implementing product behavior. This issue is limited to: MCP/tool configuration.

## Context/Architectural References

- [engineering-foundation.md](../../architecture/engineering-foundation.md) §5–6, §14–20
- [implementation-roadmap.md](../../roadmap/implementation-roadmap.md) Stage 1 item 7
- Parent: [SY-0008](SY-0008.md)
- Blockers: [SY-0009](SY-0009.md), [SY-0014](SY-0014.md)

## In Scope

- Repository `.codex/config.toml` (foundation §5 / §19)
- Read-only Context7 and Cloudflare Documentation MCP
- Specialized Playwright, pinned and disabled by default
- Documented GitHub / log / database permissions that are **not** MCP
- Secret references only (`CONTEXT7_API_KEY` name)
- Static `pnpm mcp:check`

## Out of Scope

- Work belonging to other Stage 1 issues
- Production resource creation, secret values, or real-user data
- Product features
- Starting Playwright or calling remote MCP from CI
- GitHub MCP, Cloudflare account MCP, Sentry, Figma, filesystem/shell MCP

## Implementation Requirements

- Apply only the repository areas named for this issue.
- Layers this issue does not touch: Database: None. API: None. Flutter: None. Web application code: None. Infrastructure: None.
- Record every required human decision before treating the inventory as approved architecture.

## Acceptance Criteria

- [x] Named outcome for MCP/tool configuration exists and is reviewable — [docs/development/mcp.md](../../development/mcp.md)
- [x] Stage exit criteria that this issue contributes to are satisfied or explicitly deferred (see Implementation Notes)
- [x] Tracker lint is green and relations remain a DAG — `pnpm tracker:lint`: 152 issues, 21 projects, 0 errors, 0 warnings

## Testing Requirements

Stage-wide list, applied to this issue as:

| Requirement | This issue |
|---|---|
| Clean checkout bootstrap | Deferred to [SY-0017](SY-0017.md) |
| Deterministic format/lint/test/build smoke | `pnpm verify` (includes `mcp:check`) |
| Tracker fixtures | Deferred to [SY-0013](SY-0013.md) |
| Skill trigger tests | Deferred to [SY-0014](SY-0014.md) |
| MCP inventory check | `pnpm mcp:check` — required servers, pin, forbidden servers, no secrets |

## Security/Privacy Requirements

- No secret values in Git. `CONTEXT7_API_KEY` is a name in `.env.example`.
- Playwright: isolated profile, local origins only, downloads in `/tmp/sadhanayog-playwright-mcp`, headed mode needs a human.
- No production D1/R2, browser profile, shell, secrets-manager, or Cloudflare account MCP.
- CI does not start MCP servers. `mcp:check` is static. Workflow permissions stay `contents: read` (owned by [SY-0016](SY-0016.md)).
- Fork PRs receive no MCP secrets because none are configured.

## Documentation Requirements

Documented in [mcp.md](../../development/mcp.md) and linked from setup, commands, generated-files, environment, agent-authority, troubleshooting, and the docs map.

## Rollback/Recovery

Revert `.codex/config.toml`, `.grok/config.toml`, `tools/ci/mcp-inventory.json`, `tools/ci/check-mcp.mjs`, and the docs/note. Disable a server by setting `enabled = false`. No remote resources are created.

## Implementation Notes

### Process contradiction

`work-issue` says start only when every `blocked_by` is `done`. A human named this issue while [SY-0009](SY-0009.md) and [SY-0014](SY-0014.md) are not `done`. The workspace, skills, and `.codex/` path already exist on this checkout. The contradiction is recorded; `blocked_by` is not rewritten.

Work landed on `shreyas/sy-0014-agent-instructions` because that is the current branch. Split to `shreyas/sy-0015-mcp-tool-configuration` if this must ship separately.

### What landed

- [`.codex/config.toml`](../../../.codex/config.toml) — Context7 (allowlisted read tools, optional bearer env), Cloudflare docs URL, Playwright pinned `@playwright/mcp@0.0.79` and `enabled = false`
- [`.grok/config.toml`](../../../.grok/config.toml) — same inventory for Grok (Grok does not read Codex TOML)
- [`tools/ci/mcp-inventory.json`](../../../tools/ci/mcp-inventory.json) + [`tools/ci/check-mcp.mjs`](../../../tools/ci/check-mcp.mjs)
- Named outcome: [docs/development/mcp.md](../../development/mcp.md)
- Proposed note (not authority): [`.agents/notes/proposed/process/2026-08-19-mcp-inventory.md`](../../../.agents/notes/proposed/process/2026-08-19-mcp-inventory.md)

### Human decisions (unsigned)

Recorded in mcp.md. None were decided by this issue:

1. Engineering foundation remains Proposed.
2. Grok projection is extra relative to the §5 tree.
3. Playwright stays disabled in Git.
4. No GitHub MCP; use `gh`.
5. No log or database MCP.
6. Context7 API key is optional and local.
7. Playwright pin is `0.0.79`, not `@latest`.
8. Cloudflare docs tool names are not allowlisted; the docs URL is the control.

### Stage 1 exit this issue owns

| Stage 1 exit | This issue |
|---|---|
| Understand protected MCP / agent-tool operations from documentation | Satisfied by mcp.md + agent-authority.md |
| Clone / bootstrap / run three apps / CI green | Other children |
| No product feature | Satisfied — config and docs only |

## Review Evidence

2026-08-19 on this checkout (`shreyas/sy-0014-agent-instructions`):

| Command | Result |
|---|---|
| `pnpm mcp:check` | `ok: mcp inventory, .codex/config.toml, .grok/config.toml` |
| `pnpm tracker:lint` | 152 issues, 21 projects, 0 errors, 0 warnings |
| `pnpm tracker:test` | `ok: tracker smoke` |
| `pnpm secrets:scan` | `ok: no secret-like strings` |
| `pnpm boundaries` | `ok: import boundaries` |
| unit tests (contracts/db/api/web) | 8 tests passed |
| `pnpm typecheck` | passed |
| `pnpm build` | web Vite build + Worker dry-run |
| `pnpm generated:check` | OpenAPI matches source |
| `eslint tools/ci/check-mcp.mjs` | passed |
| `prettier --check` on SY-0015 files | passed |

`pnpm verify` as a single script did **not** complete on this branch: `verify` also runs `bootstrap:test`, `bootstrap:check`, and `docs:lint`. Those fail on in-progress [SY-0017](SY-0017.md) docs that point at missing `tools/ci/local-db.mjs` and `tools/ci/check-bootstrap.mjs`, and on Prettier drift in [SY-0014](SY-0014.md) `tools/ci/lint-skills.mjs` / `skills-lib.mjs`. Those files are not this issue. Flutter analyze/test were not re-run (MCP config does not touch Dart).

## Completion Checklist

- [x] Acceptance criteria checked
- [x] Required verification commands recorded
- [ ] Independent review recorded
- [ ] Protected-path human approval recorded if applicable — `.agents/notes/` is CODEOWNERS-protected; the proposed note needs a human. Config is not a production resource.
