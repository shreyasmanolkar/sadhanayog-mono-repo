---
type: Issue
id: SY-0016
title: CI foundation
description: Create the runnable monorepo skeleton, governance, agent system and CI foundation without implementing product behavior.
status: in_review
priority: P1
estimate: null
assignee: agent
project: stage-1
milestone: Stage 1
cycle: stage-1
rank: null
tags: [feature, security]
parent: SY-0008
blocked_by: [SY-0010, SY-0011, SY-0012, SY-0013, SY-0014, SY-0015]
blocks: [SY-0129]
relates: []
resource: docs/roadmap/implementation-roadmap.md
linear_id: null
branch: shreyas/sy-0016-ci-foundation
pr: null
created: 2026-08-19
timestamp: 2026-08-19T18:53:21Z
stage: 1
security_impact: medium
---

## Objective

Create the runnable monorepo skeleton, governance, agent system and CI foundation without implementing product behavior. This issue is limited to: CI foundation.

## Context/Architectural References

- docs/architecture/engineering-foundation.md §5–6, §14–20; docs/roadmap/implementation-roadmap.md Stage 1
- Parent: SY-0008
- Stage 1: Repository and engineering foundation

## In Scope

- CI foundation
- Repository effects: Target tree in foundation §5. Empty Drizzle package and local D1 binding only. Health placeholder only.

## Out of Scope

- Work belonging to other issues in this stage
- Production resource creation, secret values, or real-user data
- Product features not named by this issue

## Implementation Requirements

- CI foundation
- Apply only the repository areas named for this stage. Write "None." for layers this issue does not touch.
- Record every required human decision before implementation proceeds.

## Acceptance Criteria

- [x] Named outcome for CI foundation exists and is reviewable
- [x] Stage exit criteria that this issue contributes to are satisfied or explicitly deferred
- [x] Tracker lint is green and relations remain a DAG

## Testing Requirements

Clean checkout bootstrap, deterministic format/lint/test/build smoke, tracker fixtures, skill trigger tests.

## Security/Privacy Requirements

CI permissions default read; fork PRs receive no secrets; artifacts exclude .dev.vars, D1/R2 data and signing files.

## Documentation Requirements

Document setup, commands, generated files, dependencies, troubleshooting, environment matrix and agent authority.

## Rollback/Recovery

Scaffold/config changes are reversible. Keep foundation changes separated by concern.

## Implementation Notes

- Named outcome: `docs/development/ci.md`. Workflow: `.github/workflows/ci.yml`. Support: `.github/CODEOWNERS`, `.github/pull_request_template.md`, `.github/dependabot.yml`. Checkers: `tools/ci/check-secrets.mjs`, `tools/ci/check-licenses.mjs`, `tools/ci/check-ci-policy.mjs` plus tests.
- Four parallel PR jobs: docs/tracker/secrets/policy; TS lint/type/test/build/drift; Flutter analyze/test/`build bundle`; dependency license policy. No path filters. No artifact uploads. `permissions: contents: read`. Checkout `persist-credentials: false`. Actions pinned by SHA. Secret scan is in-process (does not no-op when `rg` is missing).
- Database: None. API: None. Flutter application code: None. Web application code: None. Infrastructure: None. No production GitHub environments, Cloudflare resources, or secret values.
- `@types/node` `24.13.3` added to `packages/contracts` and `packages/db` so clean-checkout `tsup --dts` can satisfy `packages/config/typescript/node.json` (`types: ["node"]`). `apps/mobile/test/result_test.dart` uses `const Result` so `flutter analyze` is not red on `prefer_const_constructors`. Both are CI-green repairs, not product types.
- Human decisions recorded, not taken: CI host (proposed ADR-0006), CODEOWNERS reviewer set, product SPDX, shared development environment, GitHub branch-protection settings.
- Deferred with pointers: Playwright/axe (no product flow), D1 upgrade tests (no product schema), iOS/Android APK/IPA (Stage 17 / SY-0129), skill trigger harness (SY-0014), invalid-state tracker fixtures (SY-0013).
- Process note: `blocked_by` SY-0010–SY-0015 are not `done`. The bulk Stage 1 scaffold already contains the empty apps, tracker, docs, and skills those issues own. A human named this issue. Recorded, not a rewrite of `blocked_by`.

## Review Evidence

- `pnpm verify` (2026-08-19, worktree `sadhanayog-sy-0016`, Node 24.11.1 / pnpm 10.33.0): tracker 152 issues 0 errors; tracker smoke; docs/decision lint; in-process secret scan; CI policy; CI checker tests; license allowlist; import boundaries; eslint + prettier; typecheck (contracts/db DTS now succeeds); unit tests; web/api dry-run build; OpenAPI drift.
- Flutter 3.35.4 in `apps/mobile`: `flutter analyze` no issues; `flutter test` 2 passed; `flutter build bundle` exit 0.
- `pnpm ci:policy` asserts SHA-pinned Actions, `permissions: contents: read`, no `secrets.*`, no `pull_request_target`, no artifact uploads, `persist-credentials: false`, Node/pnpm/Flutter pins match `mise.toml`.
- GitHub required-check enablement, CODEOWNERS reviewer identity, and production environments were not changed (human GitHub settings). Independent review and protected-path approval remain the human gate.

## Completion Checklist

- [x] Acceptance criteria checked
- [x] Required verification commands recorded
- [ ] Independent review recorded
- [ ] Protected-path human approval recorded if applicable
