---
type: Issue
id: SY-0017
title: Developer bootstrap
description: Document bootstrap, local env templates, seed/reset, troubleshooting, and the environment matrix.
status: in_review
priority: P1
estimate: null
assignee: agent
project: stage-1
milestone: Stage 1
cycle: stage-1
rank: null
tags: [task]
parent: SY-0008
blocked_by: [SY-0010, SY-0011]
blocks: []
relates: []
resource: docs/roadmap/implementation-roadmap.md
linear_id: null
branch: shreyas/sy-0017-developer-bootstrap
pr: "17"
created: 2026-08-19
timestamp: 2026-08-20T00:00:00Z
stage: 1
security_impact: low
---

## Objective

Create the runnable monorepo skeleton, governance, agent system and CI foundation without implementing product behavior. This issue is limited to: Developer bootstrap.

## Context/Architectural References

- docs/architecture/engineering-foundation.md §5–6, §14–20; docs/roadmap/implementation-roadmap.md Stage 1
- Parent: SY-0008
- Stage 1: Repository and engineering foundation

## In Scope

- Developer bootstrap
- Repository effects: Target tree in foundation §5. Empty Drizzle package and local D1 binding only. Health placeholder only.

## Out of Scope

- Work belonging to other issues in this stage
- Production resource creation, secret values, or real-user data
- Product features not named by this issue

## Implementation Requirements

- Developer bootstrap
- Apply only the repository areas named for this stage. Write "None." for layers this issue does not touch.
- Record every required human decision before implementation proceeds.

## Acceptance Criteria

- [x] Named outcome for Developer bootstrap exists and is reviewable
- [x] Stage exit criteria that this issue contributes to are satisfied or explicitly deferred
- [x] Tracker lint is green and relations remain a DAG

## Testing Requirements

Clean checkout bootstrap, deterministic format/lint/test/build smoke, tracker fixtures, skill trigger tests.

## Security/Privacy Requirements

CI permissions default read; fork PRs receive no secrets; artifacts exclude .dev.vars, D1/R2 data and signing files.

## Documentation Requirements

Document setup, commands, generated files, dependencies, troubleshooting, environment matrix and agent authority.

## Rollback/Recovery

Scaffold/config changes are reversible. Keep foundation changes separated by concern.

## Implementation Notes

- Named outcome: [docs/development/README.md](../../development/README.md) plus [setup.md](../../development/setup.md), [commands.md](../../development/commands.md), [generated-files.md](../../development/generated-files.md), [dependencies.md](../../development/dependencies.md), [environment.md](../../development/environment.md), [agent-authority.md](../../development/agent-authority.md), and [troubleshooting](../../operations/troubleshooting/README.md). Root [README.md](../../../README.md) quick start points at the same flow.
- Local templates: [`.env.example`](../../../.env.example) (public names only) and [`apps/api/.dev.vars.example`](../../../apps/api/.dev.vars.example) (secret names, no values). [`tools/ci/bootstrap.sh`](../../../tools/ci/bootstrap.sh) installs pins/lockfile/Flutter pub get; it does not copy secrets.
- Synthetic seed/reset: [`tools/ci/local-db.mjs`](../../../tools/ci/local-db.mjs) via `pnpm db:seed:local` / `pnpm db:reset:local`. Only `sadhanayog-dev`. Reset requires `--confirm sadhanayog-dev`. Refuses `--remote`, forbidden names, and `SADHANAYOG_ENV=production`. Product rows are not inserted (Stage 3).
- Artifact/security check: [`tools/ci/check-bootstrap.mjs`](../../../tools/ci/check-bootstrap.mjs) asserts templates, gitignore (`.dev.vars`, sqlite, wrangler, signing), example files with no assigned secrets, and CI `permissions: contents: read` with no workflow `secrets:` or `upload-artifact`.
- Database: local D1 ledger only (`schema_migrations`). No product tables.
- API: None.
- Flutter: None. Host mapping documented (`127.0.0.1` vs Android `10.0.2.2`). No client or flavors.
- Web: None.
- Infrastructure: local templates and CI posture assertions only. No Cloudflare, DNS, identity, or GitHub environment resources.
- Required human decisions remain unsigned: engineering-foundation approval; exact tool versions (SY-0009); package licenses; whether a shared development environment is needed; protected-path reviewers beyond the CODEOWNERS placeholder. No new ADR; stack notes stay `proposed/`.
- Stage exit this issue owns: clone/bootstrap docs + `pnpm bootstrap` / `pnpm verify` / local seed-reset. Deferred: tracker invalid-state fixtures (SY-0013); skill trigger tests (SY-0014); GitHub Actions green-after-merge confirmation and iOS/Android *build* smoke (SY-0016); `flutter analyze` two `prefer_const_constructors` infos in `apps/mobile/test/result_test.dart` (SY-0011). `pnpm test:e2e` is not wired.
- Process note: `blocked_by` SY-0010 and SY-0011 were `in_review` (scaffolds already on this tree), not `done`. The issue itself had been bulk-moved to `in_review` with empty notes. A human named SY-0017. Recorded here; `blocked_by` was not rewritten.
- Rebased onto `origin/staging` after SY-0008–SY-0016 landed. Kept the Stage 1 program in [docs/development/README.md](../../development/README.md); added seed/reset, bootstrap checks, and operator docs without dropping later pin/MCP/CI/skill commands.
- GitHub-hosted `ubuntu-latest` has Java 17 on PATH; the Android pin is 25.0.1. `pnpm toolchain:check` now skips that runner default under `GITHUB_ACTIONS` so the governance job is not failed by an unused JDK. Local machines still fail if `java` is present and unpinned.

## Review Evidence

- `pnpm verify` (2026-08-19, Linux, worktree `/home/shreyas/Work/sadhanayog/sadhanayog-sy-0017`): tracker 152 issues 0 errors; tracker smoke; `bootstrap:test`; docs/decision lint; secret scan; `bootstrap:check`; import boundaries; eslint/prettier; typecheck; contracts/db/api/web tests; web/api dry-run build (Wrangler 4.124.0, local D1 `sadhanayog-dev`); OpenAPI drift check.
- `pnpm db:seed:local -- --database sadhanayog-dev`: Wrangler local execute of `0000_schema_migrations.sql` succeeded. `pnpm db:reset:local -- --database sadhanayog-dev` exited 2 without `--confirm`. `pnpm db:reset:local -- --database sadhanayog-dev --confirm sadhanayog-dev` deleted local persist and remigrated; seed reapplied.
- `cd apps/mobile && flutter test` (Flutter 3.35.4): Result + foundation shell tests passed. `flutter analyze` reports 2 info-level `prefer_const_constructors` in `test/result_test.dart`; not fixed here.
- No `.dev.vars` values, D1/R2 payloads, or signing files were committed. Local persist stays under gitignored `apps/api/.wrangler/`.
- `pnpm tracker:lint`: 152 issues, 0 errors, 0 warnings.

## Completion Checklist

- [x] Acceptance criteria checked
- [x] Required verification commands recorded
- [ ] Independent review recorded
- [ ] Protected-path human approval recorded if applicable
