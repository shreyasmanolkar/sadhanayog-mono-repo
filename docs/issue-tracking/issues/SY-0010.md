---
type: Issue
id: SY-0010
title: Empty application/package scaffolds
description: Create compiling Worker, Vite/TanStack web, Flutter iOS/Android, and contracts/db/config shells without domain features.
status: in_review
priority: P1
estimate: null
assignee: shreyas
project: stage-1
milestone: Stage 1
cycle: stage-1
rank: null
tags: [task]
parent: SY-0008
blocked_by: [SY-0009]
blocks: [SY-0016, SY-0017, SY-0026, SY-0034, SY-0045, SY-0064, SY-0072]
relates: []
resource: docs/roadmap/implementation-roadmap.md
linear_id: null
branch: shreyas/sy-0010-empty-scaffolds
pr: "10"
created: 2026-08-19
timestamp: 2026-08-19T19:57:57Z
stage: 1
security_impact: low
---

## Objective

Create the runnable monorepo skeleton, governance, agent system and CI foundation without implementing product behavior. This issue is limited to: Empty application/package scaffolds.

## Context/Architectural References

- docs/architecture/engineering-foundation.md §5–6, §14–20; docs/roadmap/implementation-roadmap.md Stage 1
- Parent: SY-0008
- Stage 1: Repository and engineering foundation

## In Scope

- Empty application/package scaffolds
- Repository effects: Target tree in foundation §5. Empty Drizzle package and local D1 binding only. Health placeholder only.

## Out of Scope

- Work belonging to other issues in this stage
- Production resource creation, secret values, or real-user data
- Product features not named by this issue

## Implementation Requirements

- Empty application/package scaffolds
- Apply only the repository areas named for this stage. Write "None." for layers this issue does not touch.
- Record every required human decision before implementation proceeds.

## Acceptance Criteria

- [x] Named outcome for Empty application/package scaffolds exists and is reviewable
- [x] Stage exit criteria that this issue contributes to are satisfied or explicitly deferred
- [x] Tracker lint is green and relations remain a DAG

## Testing Requirements

Clean checkout bootstrap, deterministic format/lint/test/build smoke, tracker fixtures, skill trigger tests.

## Security/Privacy Requirements

CI permissions default read; fork PRs receive no secrets; artifacts exclude .dev.vars, D1/R2 data and signing files.

## Documentation Requirements

Document setup, commands, generated files, dependencies, troubleshooting, environment matrix and agent authority.

## Rollback/Recovery

Scaffold/config changes are reversible. Keep foundation changes separated by concern.

## Implementation Notes

- Named outcome: compiling empty shells at `apps/api`, `apps/web`, `apps/mobile`, plus `packages/{config,contracts,db}`. No product features.
- Worker: health placeholder only (`/health/live`, `/health/ready`, `/api/v1/health/live`, `/api/v1/health/ready`). Ready is unauthenticated on purpose; protection is SY-0034. Typed D1 boot is `apps/api/src/app/db.ts`. Local D1 binding remains the placeholder in `apps/api/wrangler.jsonc`.
- Web: existing Vite/TanStack shell kept. File-based routes, shared components, feature folders, and Playwright e2e are placeholders pointing at SY-0072 / later slices.
- Flutter: dart-define `AppConfig` shell (`SADHANAYOG_ENV`, `SADHANAYOG_API_ORIGIN`) with constructor DI via `provider`. Native Android/iOS product flavors remain Stage 8 (SY-0064), as already stated in `apps/mobile/README.md`.
- Database: `packages/db` still exports only `schema_migrations`. `createDb` is now public. `fixtures/` is a placeholder. No product tables.
- Contracts: unchanged health/problem OpenAPI. Config package TypeScript presets unchanged; ESLint/Vitest presets stay SY-0011.
- Target-tree placeholders added for `apps/api/src/auth`, `apps/api/test/{contract,integration,security}`, `apps/mobile/{lib/features,lib/l10n,integration_test}`, `apps/web/{src/components,src/features,src/routes,e2e}`, `packages/db/fixtures`.
- Auth / R2 / production resources / CI workflows / product domain: None.
- Human decisions recorded, not taken: stack selections remain proposed foundation ADRs. Added proposed ADR-0006 for the Flutter shell; it is not authority until a human moves it. CI host, licenses, shared staging env, and CODEOWNERS stay sibling issues.
- Stage exit criteria this issue owns: the three empty apps compile, health placeholder exists, Drizzle package has no product tables. Bootstrap, tracker/ADR workflow, CI-from-clean-checkout, and protected-operation docs are deferred to SY-0009 / SY-0011–SY-0017.
- Process note: `blocked_by` SY-0009 is `in_review`/`in_progress` (workspace already on `staging`), not `done`, when this work started at human request. Same Stage 0 exception already sits on SY-0003–SY-0007. Recorded, not rewritten.

## Review Evidence

- `pnpm verify` (2026-08-19): tracker 152 issues, 0 errors; tracker smoke; docs/decision lint; secret scan; import boundaries; eslint/prettier; typecheck; unit tests (contracts 3, db 2, api 8, web 1); web/api dry-run build with local D1 binding; OpenAPI drift check.
- `cd apps/mobile && flutter analyze && flutter test` (Flutter 3.35.4): no analyzer issues; Result, config, and shell widget tests passed.

## Completion Checklist

- [x] Acceptance criteria checked
- [x] Required verification commands recorded
- [ ] Independent review recorded
- [ ] Protected-path human approval recorded if applicable
