---
type: Issue
id: SY-0009
title: Workspace/toolchain scaffold
description: Pin Node, pnpm, Flutter, Dart, Wrangler, and Java; create the apps/packages/content/tools workspace.
status: in_review
priority: P1
estimate: null
assignee: agent
project: stage-1
milestone: Stage 1
cycle: stage-1
rank: null
tags: [task]
parent: SY-0008
blocked_by: [SY-0002]
blocks: [SY-0010, SY-0011, SY-0012, SY-0015, SY-0026, SY-0051]
relates: []
resource: docs/roadmap/implementation-roadmap.md
linear_id: null
branch: shreyas/sy-0009-workspace-toolchain
pr: https://github.com/shreyasmanolkar/sadhanayog-mono-repo/pull/11
created: 2026-08-19
timestamp: 2026-08-19T19:56:09Z
stage: 1
security_impact: low
---

## Objective

Create the runnable monorepo skeleton, governance, agent system and CI foundation without implementing product behavior. This issue is limited to: Workspace/toolchain scaffold.

## Context/Architectural References

- docs/architecture/engineering-foundation.md §5–6, §14–20; docs/roadmap/implementation-roadmap.md Stage 1
- Parent: SY-0008
- Stage 1: Repository and engineering foundation

## In Scope

- Workspace/toolchain scaffold
- Repository effects: Target tree in foundation §5. Empty Drizzle package and local D1 binding only. Health placeholder only.

## Out of Scope

- Work belonging to other issues in this stage
- Production resource creation, secret values, or real-user data
- Product features not named by this issue

## Implementation Requirements

- Workspace/toolchain scaffold
- Apply only the repository areas named for this stage. Write "None." for layers this issue does not touch.
- Record every required human decision before implementation proceeds.

## Acceptance Criteria

- [x] Named outcome for Workspace/toolchain scaffold exists and is reviewable
- [x] Stage exit criteria that this issue contributes to are satisfied or explicitly deferred
- [x] Tracker lint is green and relations remain a DAG

## Testing Requirements

Clean checkout bootstrap, deterministic format/lint/test/build smoke, tracker fixtures, skill trigger tests.

## Security/Privacy Requirements

CI permissions default read; fork PRs receive no secrets; artifacts exclude .dev.vars, D1/R2 data and signing files.

## Documentation Requirements

Document setup, commands, generated files, dependencies, troubleshooting, environment matrix and agent authority.

## Rollback/Recovery

Scaffold/config changes are reversible. Keep foundation changes separated by concern.

## Implementation Notes

- Process note: `blocked_by` SY-0002 was `in_review` (named outcome
  `docs/discovery/repository-baseline.md` already landed), not `done`, when a
  human named this issue. Same exception already sits on SY-0003–SY-0007.
  Recorded, not rewritten. The engineering foundation document remains
  Proposed; this issue follows it and does not approve it.
- Named outcome: [`docs/development/toolchain.md`](../../development/toolchain.md).
  Canonical pins: [`tools/ci/tool-pins.json`](../../../tools/ci/tool-pins.json).
  Drift check: `pnpm toolchain:check` (`tools/ci/check-tool-pins.mjs`).
- Pins (unsigned, proposed ADR-0007): Node 24.11.1, pnpm 10.33.0, Flutter
  3.35.4, Dart 3.9.2 (Flutter-bundled; no mise `dart` tool), Java 25.0.1
  (host JDK), Wrangler 4.124.0 (`apps/api` devDependency). `mise.toml`
  comments point at the JSON table. Root `packageManager` / `engines` and
  CI workflow versions must match the table.
- Workspace tree: Git already existed. `apps/`, `packages/`, `content/`,
  `tools/{ci,tracker}` already existed from the bulk Stage 1 scaffold.
  This issue added `tools/migration/` and `tools/content/` placeholders
  only. Compiling Worker/web/Flutter shells remain SY-0010. Formatters
  remain SY-0011. CI jobs remain SY-0016. Bootstrap runbook remains
  SY-0017. `pnpm bootstrap` now runs the pin check after install.
- Database: None. No product tables.
- API: None. Health placeholder is SY-0010.
- Flutter application code: None. SDK pin only.
- Web application code: None. Workspace membership only.
- Infrastructure: None. No production DNS, D1, R2, identity tenants, or
  CI permission changes. `.gitignore` still excludes `.dev.vars`, sqlite,
  Wrangler state, and signing files; the pin checker asserts those
  patterns exist.
- Required human decisions (unsigned, also in toolchain.md): exact tool
  versions; CI host (GitHub Actions is observed); package license (no
  `LICENSE` file); shared development environment (not created); protected
  path reviewers (CODEOWNERS, SY-0016); Java 25 vs Android JDK 17 LTS;
  Windows contributor hosts; engineering-foundation approval.
- Stage exit rows this issue owns: pinned Node/pnpm/Flutter/Dart/Wrangler/Java,
  root scripts, lockfiles, documented matrix. Deferred: clean-machine
  bootstrap evidence on a macOS mobile build host (SY-0017); tracker
  invalid-state fixtures (SY-0013); skill trigger tests (SY-0014);
  compiling empty apps (SY-0010); CI job design (SY-0016).
- Pull request: https://github.com/shreyasmanolkar/sadhanayog-mono-repo/pull/11
  (`shreyas/sy-0009-workspace-toolchain` → `staging`).
- Merged `origin/staging` (SY-0008 PR #9, SY-0010 PR #10) on 2026-08-20.
  Conflicts: `docs/architecture/decisions.md`, `docs/development/README.md`,
  `docs/issue-tracking/log.md`, `docs/issue-tracking/board.html`. Staging
  already used ADR-0006 for the Flutter shell, so the pin table is ADR-0007.
  The Stage 1 program from SY-0008 is kept; this issue only added pin-table
  rows. Compiling shells that arrived with that merge remain SY-0010's.

## Review Evidence

- `pnpm toolchain:check` (2026-08-19, Linux x64): `ok: toolchain pins node 24.11.1, pnpm 10.33.0, flutter 3.35.4, dart 3.9.2, java 25.0.1, wrangler 4.124.0`.
- `bash tools/ci/bootstrap.sh` (2026-08-19, isolated worktree at `68534cd`):
  mise.toml is untrusted in a new worktree (`mise trust` required);
  bootstrap warns and continues; `pnpm install --frozen-lockfile` and
  `pnpm toolchain:check` green; `flutter pub get` green.
- `pnpm verify` (2026-08-19): tracker 152 issues, 0 errors, 0 warnings;
  tracker smoke; pin check; docs/decision lint; secret scan; import
  boundaries; eslint/prettier; typecheck; unit tests; web/api dry-run
  build; OpenAPI drift. Flutter analyze/test not re-run; this issue did
  not change `apps/mobile` sources.
- After merge `d26aadf` (2026-08-20): `pnpm verify` green on the resolved
  tree (tracker 152 / 0 errors; pin check; docs/decision lint; secret
  scan; boundaries; eslint/prettier; typecheck; unit tests including API
  contract health 8; web/api dry-run; OpenAPI drift). PR mergeable.
- Tracker fixtures and skill trigger tests not added here; deferred to
  SY-0013 and SY-0014.
- No `.dev.vars` values, D1/R2 payloads, or signing files were committed.

## Completion Checklist

- [x] Acceptance criteria checked
- [x] Required verification commands recorded
- [ ] Independent review recorded
- [ ] Protected-path human approval recorded if applicable
