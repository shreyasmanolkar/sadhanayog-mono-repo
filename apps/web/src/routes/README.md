# `apps/web/src/routes`

File-based TanStack Router routes land in Stage 9 (SY-0072). The Stage 1 shell
uses the code-based router in `src/app/router.tsx`. Generated `routeTree.gen.ts`
stays gitignored until that issue.
